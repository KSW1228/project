---
# ===========================================================
#  모든 호스트 공통 변수 (group_vars/all)
# ===========================================================

# ── FTP 공유 폴더(/pub) 접근용 IPA 그룹 ──
#   .218(IPA 서버)에서 그룹 생성 + 멤버 추가,
#   .114(FTP)에서 /pub 의 소유 그룹으로 사용 → 양쪽이 같은 이름을 봐야 함.
ftp_share_group: ftpshare
